# Core backend runtime dependencies.
#
# This file is the contract for the future lightweight backend image. It must
# not include browser automation, local embedding, or perception/ASR stacks.

# Web framework
fastapi==0.136.3
uvicorn[standard]==0.48.0
python-multipart>=0.0.9
bcrypt>=4.1.0

# Database
sqlalchemy[asyncio]==2.0.50
aiosqlite==0.22.1
alembic==1.18.4

# Data validation
pydantic==2.13.4
pydantic-settings==2.14.2

# HTTP and non-browser scraping
httpx==0.28.1
beautifulsoup4==4.14.3
lxml>=5.3.0

# Scheduling
apscheduler==3.11.2

# Utilities
icalendar>=5.0.0
jinja2==3.1.6
python-dotenv==1.2.2
rapidfuzz==3.14.5
pyyaml==6.0.3
aiosmtplib==5.1.2

# Agentic pipeline (LangGraph)
# Keep checkpoint packages explicit so SQLite persistence stays on patched
# serializers and security audits do not silently resolve vulnerable versions.
langgraph>=1.0.10,<2.0
langgraph-checkpoint>=4.0.0,<5.0
langgraph-checkpoint-sqlite>=3.0.0,<4.0

# LangChain: provider-agnostic LLM abstraction
# anthropic is NOT pinned here; langchain-anthropic controls the compatible version.
# Pinning it separately caused ResolutionImpossible conflicts during Docker build.
langchain>=1.3.9,<2.0
langchain-core>=1.2.31,<2.0
langchain-anthropic>=1.0.0,<2.0
langchain-openai>=1.1.14,<2.0
langchain-google-genai>=3.0.0,<5.0
langchain-ollama>=1.0.0,<2.0

# Document parsing (CV upload)
python-docx>=1.1.0
pypdf>=4.0.0
