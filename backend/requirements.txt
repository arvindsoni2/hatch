# ── Web framework ────────────────────────────────────────────────────────────
fastapi==0.136.3
uvicorn[standard]==0.48.0
python-multipart>=0.0.9

# ── Database ──────────────────────────────────────────────────────────────────
sqlalchemy[asyncio]==2.0.50
aiosqlite==0.22.1
alembic==1.18.4

# ── Data validation ───────────────────────────────────────────────────────────
pydantic==2.13.4
pydantic-settings==2.14.1

# ── HTTP & scraping ───────────────────────────────────────────────────────────
httpx==0.28.1
beautifulsoup4==4.14.3
playwright==1.60.0
lxml>=5.3.0

# ── Scheduling ────────────────────────────────────────────────────────────────
apscheduler==3.11.2

# ── Utilities ─────────────────────────────────────────────────────────────────
icalendar>=5.0.0
jinja2==3.1.6
python-dotenv==1.2.2
rapidfuzz==3.14.5
pyyaml==6.0.3
aiosmtplib==3.0.1

# ── Agentic pipeline (LangGraph) ──────────────────────────────────────────────
# SEC-9 MIGRATION NOTE: langgraph 1.x (expected Q3 2026) will rename
# StateGraph checkpointing API. Track: https://github.com/langchain-ai/langgraph
# When upgrading beyond 0.x, audit AgentOrchestrator and all graph definitions.
langgraph>=0.2.28,<0.4

# ── Vector store ──────────────────────────────────────────────────────────────
chromadb>=0.5.20,<0.6

# ── LangChain — provider-agnostic LLM abstraction ────────────────────────────
# anthropic is NOT pinned here — langchain-anthropic controls the compatible version.
# Pinning it separately caused ResolutionImpossible conflicts during Docker build.
langchain>=0.3.0,<0.4
langchain-core>=0.3.0,<0.4
langchain-anthropic>=0.3.0
langchain-openai>=0.2.0
langchain-google-genai>=2.0.0
langchain-ollama>=0.2.0

# ── Document parsing (CV upload) ──────────────────────────────────────────────
python-docx>=1.1.0
pypdf>=4.0.0

# ── Semantic scoring (local embeddings — no API cost) ─────────────────────────
# Pulls in torch (CPU build via --extra-index-url in Dockerfile). ~400 MB.
sentence-transformers>=3.0,<4.0

# ── Dev / test (also installed in container for make test) ────────────────────
ruff==0.15.15
pytest==8.3.3
pytest-asyncio==0.24.0
pytest-cov>=5.0.0
pytest-httpx==0.35.0
factory-boy==3.3.3
