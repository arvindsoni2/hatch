# Perception stack — ASR, voice emotion, TTS
# Installed in a separate Docker layer so base image rebuilds don't re-fetch these.
# Phase A: ASR only (faster-whisper + CTranslate2 CPU backend)
# Phase B adds: onnxruntime, transformers (audeering voice emotion)
# Phase E adds: piper-tts or kokoro
faster-whisper>=1.0.3
# tokenizers 0.20.x has a broken Rust build on some Linux platforms (DecodeStream missing),
# which breaks transformers 4.46.x GPT2TokenizerFast at import time. Pin to 0.19.x.
tokenizers>=0.19.0,<0.20
