# Perception stack — ASR, voice emotion, TTS
# Installed in a separate Docker layer so base image rebuilds don't re-fetch these.
# Phase A: ASR (faster-whisper + CTranslate2 CPU backend)
# Phase B: voice emotion (transformers + torch for audeering wav2vec2)
# Phase E adds: piper-tts or kokoro
faster-whisper>=1.0.3
# tokenizers 0.20.x has a broken Rust build on some Linux platforms (DecodeStream missing),
# which breaks transformers 4.46.x GPT2TokenizerFast at import time. Pin to 0.19.x.
tokenizers>=0.19.0,<0.20
# Phase B — audeering dimensional voice emotion (wav2vec2 regression, CPU)
# SEC-9 MIGRATION NOTE: transformers 5.x (expected late 2026) will remove
# the Pipeline API fallback and change model loading. Track:
# https://github.com/huggingface/transformers/releases
# When upgrading, audit perception_factory.py, voice_emotion_analyser.py.
transformers>=4.35.0
torch>=2.0.0
